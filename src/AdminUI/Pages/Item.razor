@page "/item"
@*@inject CatalogAPI.Domain.Interfaces.ICatalogService CatalogService*@
@inject NavigationManager NavigationManager

<h3>Item</h3>
<EditForm Model="@item" OnValidSubmit="HandleRegisrtion">
    <div class="form-group">
        <label for="productName">Product Name</label>
        <InputText id="productName" @bind-Value="item.Name" class="form-control" />
    </div>

    <div class="form-group">
        <label for="description">Description</label>
        <InputText id="description" @bind-Value="item.Description" class="form-control" />
    </div>

    <div class="form-group">
        <label for="price">Price</label>
        <InputNumber id="price" @bind-Value="item.Price" class="form-control" />
    </div>

    <button type="submit" class="btn btn-primary">Opslaan</button>
</EditForm>


@code {

    CatalogAPI.Domain.Models.CatalogItem item = new CatalogAPI.Domain.Models.CatalogItem();

    void HandleRegisrtion()
    {
        Console.WriteLine($"{item.Name}");

        NavigationManager.NavigateTo("");
    }

}
